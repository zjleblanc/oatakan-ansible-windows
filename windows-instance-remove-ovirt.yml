---
- name: remove vms
  hosts: all
  gather_facts: false
  connection: local
  become: false
  vars_files:
    - win-nodes-ovirt.yml

  roles:
    - role: ansible-role-ovirt
      role_action: deprovision
      delegate_to: localhost
